<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="text/javascript" src="https://code.jquery.com/jquery-1.7.1.min.js"></script>
    <style>
        th {
            border: solid 1px rgb(22, 230, 101);
        }

        table {
            border-collapse: collapse;
            /* table-layout: fixed; */
            /* width: 310px; */
        }

        /* th{
            border: 1px;
        } */

        table td {
            border: solid 1px rgb(22, 230, 101);
            width: 100px;
            word-wrap: break-word;
        }

        .no-border {
            border: 0px;
        }
    </style>

    <script>
        function removeDraft(campaign_key, campaign_name, subjectofemail, email_body, campaign_type, whomtosend) {
            console.log("from remove ", campaign_name);
            if (!window.confirm("are you sure to remove this draft?")) {
                console.log("no");
            } else {
                console.log('yes');
                //now i will send request to delete the campaign from scheduling and save as draafts
                var cdetails = {
                    "campaign_key": `${campaign_key}`,
                }
                console.log(cdetails);
                let options = {
                    method: 'POST',
                    headers: {
                        'Content-Type':
                            'application/json;charset=utf-8'
                    },
                    body: JSON.stringify(cdetails)
                }
                // api for making post requests
                let fetchRes = fetch(
                    "/Remove-Draft",
                    options);
                fetchRes.then(res =>
                    res.json()).then(d => {
                        console.log(d);
                        if (d.response_status == "1") {
                            document.location.reload();
                        } else {
                            window.alert("Can not remove this campaign Now , due to server error");
                        }
                    })
            }
        }
        function scheduleDraft(campaign_key, campaign_name, subjectofemail, email_body, campaign_type, whomtosend) {
            console.log("from schedule ", campaign_name);
            if (!window.confirm("To Schedule This Draft Press Yes!")) {
                console.log("no");
            } else {
                console.log('yes');
                //now i will send request to delete the campaign from scheduling and save as draafts
                var cdetails = {
                    "campaign_key": `${campaign_key}`,
                }
                console.log(cdetails);
                let options = {
                    method: 'POST',
                    headers: {
                        'Content-Type':
                            'application/json;charset=utf-8'
                    },
                    body: JSON.stringify(cdetails)
                }
                // api for making post requests
                let fetchRes = fetch(
                    "/campaigns/user/review_email",
                    options);
                fetchRes.then(res =>
                    res.json()).then(d => {
                        console.log(d);
                        if (d.response_status == "1") {
                            document.location.reload();
                        } else {
                            window.alert("Can not remove this campaign Now , due to server error");
                        }
                    })
                // const urlis = "/campaigns/user/review_email"
                // console.log(urlis);
                // $.ajax({
                //     method: 'post',
                //     url: urlis,
                //     data: cdetails,
                //     success: (res) => {
                //         // do something
                //         console.log(res);
                //     }
                // });
            }
        }
    </script>
    <link href="/css-files/campaigns.css" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>campaigns ejs</title>
</head>

<body>
    <div class="conatainer">
        <div class="side_bar bg-light">
            <%- include('../common/sidebar')%>
        </div>
        <div class="main_area">
            <div class="main_header">
                <div class="camp_heading w-100">
                    <div class="profile_plan h3 ">
                        <%=data.c_name%>
                    </div>
                    <div class="profile_info">
                        <div class="profile_container">
                            <div class="profile_info_name h5 ">
                                <%=data.name%>
                            </div>
                            <div class="profile_info_email h5 ">
                                <%=data.email%>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="camp_heading2 ml-4 w-100">
                    <div class="profile_plan h2 ">
                        Campaigns
                    </div>
                    <div class="profile_info">
                        <form id="main_form" method="post" action="/campaigns/create">
                            <button type="submit" class="btn btn-primary btn-lg">Create New Campaign</button>
                        </form>
                    </div>
                </div>
                <!-- <div class="title_name h2">Campaigns</div> -->
                <div class="state ">
                    <%- include('../common/camp-navbar')%>

                </div>
            </div>

            <div class="work_area">
                <div class="work_container text-center m-4 p-4">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="h2  card-title">Drafts</h4>
                            <p class="card-description"></p>
                            <%if(data.draftdetails){%>
                                <div class="table-responsive">
                                    <table class="table" id="draft-table">
                                        <thead>
                                            <tr>
                                                <th class="no-border">To Schedule Camp.</th>
                                                <th>campaign name</th>
                                                <th>subject of email</th>
                                                <th>Email Body</th>
                                                <th>Whom to send</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% for (var i=0; i < data.draftdetails.length; i++) { %>
                                                <tr class="no-border">
                                                    <td class="no-border">
                                                        <form action="/campaigns/user/review_email" method="post">
                                                            <div class="form-group col " style="display: none;">
                                                                <label for="campaign_key">Campaign KEY</label>
                                                                <input type="text" name="campaign_key" class="form-control" id="campaign_key" value="<%=data.draftdetails[i].campaign_key%>"
                                                                    readonly>
                                                            </div>
                                                            <input type="submit" value="Schedule">
                                                        </form>
                                                    </td>
                                                    <td>
                                                        <%=data.draftdetails[i].campaign_name%>
                                                    </td>
                                                    <td>
                                                        <%=data.draftdetails[i].subjectofemail%>
                                                    </td>
                                                    <td>
                                                        <%=data.draftdetails[i].email_body%>
                                                    </td>

                                                    <td>
                                                        <%=data.draftdetails[i].whomtosend%>
                                                    </td>
                                                    <td class="no-border">
                                                        <input
                                                            onclick="removeDraft(`<%=data.draftdetails[i].campaign_key%>`,`<%=data.draftdetails[i].campaign_name%>`, `<%=data.draftdetails[i].subjectofemail%>`,`<%=data.draftdetails[i].email_body%>`,`<%=data.draftdetails[i].campaign_type%>`, `<%=data.draftdetails[i].whomtosend%>`)"
                                                            type="button" value="remove">
                                                    </td>
                                                </tr>
                                                <%}%>
                                        </tbody>
                                    </table>
                                </div>
                                <%}%>
                                    <%if(!data.draftdetails){%>
                                        <form id="main_form" method="post" action="/campaigns/create">
                                            <div class="mb-3" name="email">
                                                <label class="h3" for="exampleInputEmail1" class="form-label">Your
                                                    drafts-box is
                                                    Empty</label>

                                            </div>
                                            <button type="submit" class="btn btn-primary">Create New Campaign</button>
                                        </form>
                                        <%}%>
                        </div>
                    </div>


                </div>
            </div>
        </div>
    </div>


</body>

</html>